# 📋 快速命令参考

## 🚀 日常部署命令

### 方法 1：使用一键部署脚本（推荐）⭐

```bash
cd "/Users/hexiaoxiao/Desktop/Model Selection System"
./🎮一键部署脚本.sh
```

**功能**：
- ✅ 自动检测更改
- ✅ 分析影响范围（前端/后端）
- ✅ 引导输入提交信息
- ✅ 自动提交并推送
- ✅ 显示部署进度
- ✅ 可选：等待并验证部署

---

### 方法 2：标准 Git 命令

```bash
# 查看当前更改
git status

# 添加所有更改
git add .

# 提交（替换提交信息）
git commit -m "feat: 你的功能描述"

# 推送到 GitHub（触发自动部署）
git push origin main
```

---

## 🔍 检查部署状态

### 运行部署检查脚本

```bash
./scripts/check-deployment.sh
```

输出示例：
```
✅ 前端部署成功 (HTTP 200)
✅ 后端部署成功
✅ CORS 已配置
🎉 部署验证完成！
```

---

### 快速检查前端

```bash
curl -I https://model-selection-frontend.pages.dev
```

### 快速检查后端

```bash
curl https://model-selection-backend.onrender.com/api/health
```

---

## 📊 查看部署日志

### GitHub Actions

```bash
# 浏览器打开
open https://github.com/你的用户名/Model-Selection-System/actions

# 或使用 GitHub CLI（如果安装了）
gh run list
gh run view
```

---

## 🔄 常用 Git 操作

### 更新本地代码

```bash
git pull origin main
```

### 查看提交历史

```bash
git log --oneline -10
```

### 撤销上次提交（保留修改）

```bash
git reset --soft HEAD~1
```

### 查看远程仓库信息

```bash
git remote -v
```

---

## 🎯 提交信息规范

使用语义化提交信息：

```bash
# 新功能
git commit -m "feat: 添加用户管理功能"

# Bug 修复
git commit -m "fix: 修复登录失败问题"

# 样式修改
git commit -m "style: 更新按钮颜色"

# 文档更新
git commit -m "docs: 更新部署文档"

# 性能优化
git commit -m "perf: 优化数据库查询"

# 重构
git commit -m "refactor: 重构用户服务"

# 测试
git commit -m "test: 添加单元测试"

# 配置
git commit -m "chore: 更新依赖版本"
```

---

## 💻 本地开发命令

### 启动前端开发服务器

```bash
cd frontend
npm run dev
# 访问 http://localhost:5173
```

### 启动后端开发服务器

```bash
cd backend
npm run dev
# API 运行在 http://localhost:5001
```

### 同时启动前后端（新终端窗口）

```bash
# 终端 1 - 后端
cd backend && npm run dev

# 终端 2 - 前端
cd frontend && npm run dev
```

---

## 🧪 测试命令

### 运行前端测试

```bash
cd frontend
npm run test
```

### 运行后端测试

```bash
cd backend
npm run test
```

### 运行 E2E 测试

```bash
cd frontend
npm run cypress:open
```

---

## 📦 构建命令

### 构建前端

```bash
cd frontend
npm run build
# 输出到 frontend/dist/
```

### 预览构建结果

```bash
cd frontend
npm run preview
# 访问 http://localhost:4173
```

---

## 🔧 环境配置

### 检查 Node 版本

```bash
node --version  # 应该 >= 16.0.0
npm --version   # 应该 >= 8.0.0
```

### 安装依赖

```bash
# 前端
cd frontend && npm install

# 后端
cd backend && npm install
```

### 更新依赖

```bash
# 检查过时的包
npm outdated

# 更新所有包
npm update
```

---

## 🗄️ 数据库命令

### 初始化生产数据

```bash
cd backend
npm run init:production
```

### 验证数据完整性

```bash
cd backend
npm run verify:all
```

---

## 🔐 安全相关

### 生成 JWT Secret

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 生成随机密码

```bash
openssl rand -base64 32
```

---

## 🌐 网络诊断

### 测试前端连接

```bash
curl -I https://model-selection-frontend.pages.dev
```

### 测试后端连接

```bash
curl https://model-selection-backend.onrender.com/api/health
```

### 测试 CORS

```bash
curl -H "Origin: https://model-selection-frontend.pages.dev" \
     -I https://model-selection-backend.onrender.com/api/health
```

### 检查 DNS

```bash
nslookup model-selection-frontend.pages.dev
```

---

## 🎨 代码质量

### 运行 ESLint

```bash
cd frontend
npm run lint
```

### 自动修复 ESLint 错误

```bash
cd frontend
npm run lint:fix
```

---

## 📁 文件操作

### 查看项目结构

```bash
tree -L 2 -I 'node_modules|dist'
```

### 查找文件

```bash
find . -name "*.jsx" -type f
```

### 统计代码行数

```bash
find frontend/src -name "*.jsx" | xargs wc -l
```

---

## 🔄 分支管理

### 创建新分支

```bash
git checkout -b feature/new-feature
```

### 切换分支

```bash
git checkout main
```

### 合并分支

```bash
git checkout main
git merge feature/new-feature
```

### 删除分支

```bash
git branch -d feature/new-feature
```

---

## 🚨 紧急回滚

### 回滚到上一个版本

```bash
# 1. 查看提交历史
git log --oneline

# 2. 回滚到指定提交
git revert <commit-hash>

# 3. 推送
git push origin main
```

### 强制回滚（谨慎使用）

```bash
# 回滚到指定提交
git reset --hard <commit-hash>

# 强制推送（会触发重新部署）
git push origin main --force
```

⚠️ **注意**：强制推送会覆盖远程历史，请谨慎使用！

---

## 📊 性能监控

### 检查前端性能

```bash
cd frontend
npm run build
# 查看构建报告中的文件大小
```

### 分析打包体积

```bash
cd frontend
npx vite-bundle-visualizer
```

---

## 🛠️ 常见问题快速修复

### 清除 npm 缓存

```bash
npm cache clean --force
```

### 重新安装依赖

```bash
rm -rf node_modules package-lock.json
npm install
```

### 清除 Git 缓存

```bash
git rm -r --cached .
git add .
git commit -m "chore: 清除缓存"
```

---

## 📞 帮助命令

### Git 帮助

```bash
git --help
git commit --help
```

### npm 帮助

```bash
npm help
npm help install
```

---

## ⚡ 快捷别名（可选）

添加到 `~/.zshrc` 或 `~/.bashrc`：

```bash
# 快速部署
alias deploy="cd '/Users/hexiaoxiao/Desktop/Model Selection System' && ./🎮一键部署脚本.sh"

# 快速检查
alias check-deploy="cd '/Users/hexiaoxiao/Desktop/Model Selection System' && ./scripts/check-deployment.sh"

# 快速启动前端
alias dev-fe="cd '/Users/hexiaoxiao/Desktop/Model Selection System/frontend' && npm run dev"

# 快速启动后端
alias dev-be="cd '/Users/hexiaoxiao/Desktop/Model Selection System/backend' && npm run dev"

# Git 快捷方式
alias gs="git status"
alias ga="git add ."
alias gc="git commit -m"
alias gp="git push origin main"
```

使用方法：
```bash
# 重新加载配置
source ~/.zshrc

# 使用别名
deploy
check-deploy
dev-fe
dev-be
```

---

## 📱 移动端测试

### 在移动设备上测试

```bash
# 1. 获取本地 IP
ifconfig | grep "inet "

# 2. 启动开发服务器
cd frontend && npm run dev

# 3. 在手机浏览器访问
# http://你的IP:5173
```

---

## 🎉 完整工作流示例

```bash
# 1. 更新代码
git pull origin main

# 2. 创建新功能分支
git checkout -b feature/user-profile

# 3. 开发（修改代码）
# ...

# 4. 本地测试
cd frontend && npm run dev
cd backend && npm run dev

# 5. 确认无误后合并到 main
git checkout main
git merge feature/user-profile

# 6. 部署（使用一键脚本）
./🎮一键部署脚本.sh

# 7. 等待 3-5 分钟

# 8. 验证部署
./scripts/check-deployment.sh

# 9. 完成！🎉
```

---

## 📚 更多资源

- **详细配置**: `自动部署配置指南.md`
- **快速开始**: `快速部署指南-Cloudflare-Render.md`
- **工作流详解**: `⚡自动部署工作流程详解.md`
- **配置检查**: `🚀自动部署-配置检查清单.md`

---

**提示**：把这个文件加入书签，随时查阅！📖

