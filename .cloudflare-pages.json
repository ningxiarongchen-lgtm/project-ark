{
  "build": {
    "command": "cd frontend && npm install && npm run build",
    "cwd": "/",
    "watchPaths": [
      "frontend/**"
    ]
  },
  "preview": {
    "command": "cd frontend && npm run preview"
  },
  "routes": [
    {
      "serve": "/index.html",
      "status": 200
    }
  ]
}
