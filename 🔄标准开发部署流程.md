# 🔄 标准开发部署流程

**建立日期**: 2025-11-06

---

## 📋 标准工作流程

```
1. 开发代码 ✍️
   ↓
2. 本地测试 🧪
   ↓
3. 测试通过 ✅
   ↓
4. 推送到 Git 📤
   ↓
5. 自动部署 🚀
   - Cloudflare Pages (smart-system) - 前端
   - Render (project-ark) - 后端
   ↓
6. 验证部署 ✅
   ↓
7. 完成 🎉
```

---

## 🎯 详细步骤

### 第 1 步：开发代码

```bash
cd "/Users/hexiaoxiao/Desktop/Model Selection System"

# 修改前端
vim frontend/src/...

# 修改后端
vim backend/...
```

---

### 第 2 步：本地测试

#### 启动开发环境

```bash
# 终端1 - 启动后端
cd backend
npm run dev

# 终端2 - 启动前端
cd frontend
npm run dev
```

#### 访问测试

```bash
# 浏览器访问
open http://localhost:5173
```

#### 测试清单

- [ ] 前端页面正常加载
- [ ] 样式显示正确
- [ ] 功能正常工作
- [ ] 无控制台错误
- [ ] API 调用成功
- [ ] 数据显示正确

---

### 第 3 步：测试通过

确认所有功能正常后，继续下一步。

---

### 第 4 步：推送到 Git

#### 使用一键部署脚本（推荐）⭐

```bash
cd "/Users/hexiaoxiao/Desktop/Model Selection System"
./🎮一键部署脚本.sh
```

**脚本会自动**：
1. ✅ 检测文件变更
2. ✅ 分析影响范围（前端/后端）
3. ✅ 引导输入提交信息
4. ✅ 自动提交并推送
5. ✅ 显示部署进度

#### 或使用标准 Git 命令

```bash
# 查看变更
git status

# 添加所有变更
git add .

# 提交（使用语义化消息）
git commit -m "feat: 描述你的功能"

# 推送到 GitHub
git push origin main
```

---

### 第 5 步：自动部署（自动完成）

**推送后自动触发**：

```
GitHub 接收推送
    ↓
GitHub Actions 启动
    ↓
┌─────────────────────┐
│  前端部署           │  后端部署
│  ↓                  │  ↓
│  Cloudflare Pages   │  Render
│  (smart-system)     │  (project-ark)
│  ↓                  │  ↓
│  构建 (2-3分钟)     │  构建 (3-5分钟)
└─────────────────────┘
    ↓
部署完成 ✅
```

**查看进度**：
- GitHub Actions: https://github.com/ningxiarongchen-lgtm/project-ark/actions
- Cloudflare: https://dash.cloudflare.com/
- Render: https://dashboard.render.com/

---

### 第 6 步：验证部署（5-6分钟后）

#### 使用自动验证脚本

```bash
cd "/Users/hexiaoxiao/Desktop/Model Selection System"
./scripts/check-deployment.sh
```

#### 或手动验证

```bash
# 检查前端
curl -I https://smart-system.pages.dev
open https://smart-system.pages.dev

# 检查后端
curl https://project-ark.onrender.com/api/health
```

#### 验证清单

- [ ] 前端可访问 (smart-system.pages.dev)
- [ ] 后端 API 正常 (project-ark.onrender.com)
- [ ] 登录功能正常
- [ ] 新功能已上线
- [ ] 无明显错误

---

### 第 7 步：完成

🎉 **新版本已上线！**

用户可以访问：
- **前端**: https://smart-system.pages.dev
- **后端**: https://project-ark.onrender.com

---

## ⚡ 快速命令参考

### 完整开发周期（一键完成）

```bash
# 1. 开发代码
# ... 修改文件 ...

# 2. 本地测试
cd backend && npm run dev &
cd frontend && npm run dev

# 3. 测试通过后，推送并部署
cd "/Users/hexiaoxiao/Desktop/Model Selection System"
./🎮一键部署脚本.sh

# 4. 等待 5-6 分钟

# 5. 验证部署
./scripts/check-deployment.sh
```

---

## 📝 提交信息规范

使用语义化提交：

```bash
# 新功能
git commit -m "feat: 添加用户管理功能"

# Bug 修复
git commit -m "fix: 修复登录失败问题"

# 样式调整
git commit -m "style: 更新按钮样式"

# 性能优化
git commit -m "perf: 优化数据库查询"

# 文档更新
git commit -m "docs: 更新 API 文档"

# 代码重构
git commit -m "refactor: 重构用户服务"

# 测试
git commit -m "test: 添加单元测试"

# 构建配置
git commit -m "chore: 更新依赖版本"
```

---

## 🔍 部署状态监控

### 实时查看部署

```bash
# 打开 GitHub Actions
open https://github.com/ningxiarongchen-lgtm/project-ark/actions

# 查看最新部署
gh run list --limit 5  # 需要安装 GitHub CLI
```

### 部署状态指示

| 状态 | 说明 |
|------|------|
| 🟡 黄色 | 部署进行中 |
| ✅ 绿色 | 部署成功 |
| ❌ 红色 | 部署失败 |

---

## 🚨 如果部署失败

### 1. 查看错误日志

访问 GitHub Actions 查看具体错误：
https://github.com/ningxiarongchen-lgtm/project-ark/actions

### 2. 常见问题

**前端构建失败**：
```bash
# 检查本地构建
cd frontend
npm run build

# 如果有错误，修复后重新推送
```

**后端部署失败**：
```bash
# 检查语法错误
cd backend
npm run dev

# 检查环境变量配置
```

### 3. 回滚到上一版本

```bash
# 查看提交历史
git log --oneline -5

# 回滚到上一个版本
git revert HEAD
git push origin main
```

### 4. 重新触发部署

```bash
# 创建空提交触发部署
git commit --allow-empty -m "chore: retry deployment"
git push origin main
```

---

## 📊 部署时间参考

| 阶段 | 时间 |
|------|------|
| 代码开发 | 根据功能复杂度 |
| 本地测试 | 5-10 分钟 |
| Git 推送 | < 1 分钟 |
| 前端构建 | 2-3 分钟 |
| 后端构建 | 3-5 分钟 |
| 总部署时间 | 5-6 分钟 |
| **总计** | **10-20 分钟** |

---

## ✅ 最佳实践

### 1. 开发前

- [ ] 拉取最新代码：`git pull origin main`
- [ ] 创建功能分支（可选）：`git checkout -b feature/xxx`

### 2. 开发中

- [ ] 频繁保存代码
- [ ] 阶段性提交到本地
- [ ] 保持代码整洁

### 3. 测试时

- [ ] 测试所有相关功能
- [ ] 测试边界情况
- [ ] 检查控制台无错误
- [ ] 测试不同浏览器（如需要）

### 4. 部署前

- [ ] 确认所有测试通过
- [ ] 检查无语法错误
- [ ] 确认无敏感信息（密码、密钥）
- [ ] 写清楚提交信息

### 5. 部署后

- [ ] 等待部署完成
- [ ] 验证功能正常
- [ ] 通知团队（如需要）
- [ ] 监控用户反馈

---

## 🔄 分支策略（可选）

### 简单模式（当前使用）

```
main 分支
  ↓
直接开发 → 测试 → 推送 → 自动部署
```

### 进阶模式（团队协作）

```
main (生产环境)
  ↑
  合并
  ↑
dev (开发环境)
  ↑
  合并
  ↑
feature/* (功能分支)
  ↑
本地开发
```

---

## 📞 快速帮助

### 需要回滚？

```bash
git revert HEAD
git push origin main
```

### 需要查看部署历史？

```bash
git log --oneline -10
```

### 需要比较版本差异？

```bash
git diff HEAD~1 HEAD
```

### 需要强制重新部署？

```bash
git commit --allow-empty -m "chore: force redeploy"
git push origin main
```

---

## 🎯 记住这个流程

```
开发 → 测试 → 推送 → 自动部署 → 验证 → 完成
✍️    🧪    📤    🚀        ✅      🎉
```

**每次都按这个顺序，确保质量！** ✨

---

## 📚 相关文档

| 文档 | 用途 |
|------|------|
| `🎮一键部署脚本.sh` | 快速推送和部署 |
| `scripts/check-deployment.sh` | 验证部署状态 |
| `📋快速命令参考.md` | 常用命令速查 |
| `⚡自动部署工作流程详解.md` | 了解原理 |

---

**遵循这个流程，确保每次部署都稳定可靠！** 🚀

